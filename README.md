# Test Website

A one-page 3D creator demo site built from the `3d-creator-portfolio` Codex skill.

## Local preview

Open `index.html` directly, or run a static server from this folder.

## Deploy

This is a static site. Vercel can deploy it directly from the repository root with no build command.
